# Server
server.port=${PORT:8081}
server.servlet.context-path=${SERVER_SERVLET_CONTEXT_PATH:/api}

# Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/cahcet2_placement?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Afsal@2627}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}

# JPA
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT:org.hibernate.dialect.MySQL8Dialect}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL:true}

# JWT
app.jwt.secret=${APP_JWT_SECRET:CAHCETPlacementSystemSecretKey2024SuperSecureBase64EncodedString12345}
app.jwt.expiration=${APP_JWT_EXPIRATION:86400000}

# File Upload
spring.servlet.multipart.max-file-size=${SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE:10MB}
app.file.upload-dir=${APP_FILE_UPLOAD_DIR:./uploads/resumes}

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# Email (SMTP - configure with your Gmail credentials)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:afsalsharma43@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:ftghdfhqftwvrpsz}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.debug=${SPRING_MAIL_PROPERTIES_MAIL_DEBUG:false}

# College branding for emails
app.email.from=${APP_EMAIL_FROM:CAHCET Placement Cell <your-email@gmail.com>}
app.email.college-name=${APP_EMAIL_COLLEGE_NAME:C. Abdul Hakeem College of Engineering and Technology}
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}

# Resume serving
app.file.serve-url=${APP_FILE_SERVE_URL:/api/files/resume}

# Logging
logging.level.com.cahcet=${LOGGING_LEVEL_COM_CAHCEt:DEBUG}
logging.level.org.springframework.security=${LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY:DEBUG}

# OTP Configuration
app.otp.expiry-minutes=10

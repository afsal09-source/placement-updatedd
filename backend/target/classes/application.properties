# Server
server.port=${PORT:8081}
server.servlet.context-path=/api

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/cahcet2_placement?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Afsal@2627
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=CAHCETPlacementSystemSecretKey2024SuperSecureBase64EncodedString12345
app.jwt.expiration=86400000

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.file.upload-dir=./uploads/resumes

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Email (SMTP - configure with your Gmail credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=afsalsharma43@gmail.com
spring.mail.password=ftghdfhqftwvrpsz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.debug=false

# College branding for emails
app.email.from=CAHCET Placement Cell <your-email@gmail.com>
app.email.college-name=C. Abdul Hakeem College of Engineering and Technology
app.frontend-url=http://localhost:5173

# Resume serving
app.file.serve-url=/api/files/resume

# Logging
logging.level.com.cahcet=DEBUG
logging.level.org.springframework.security=DEBUG

# OTP Configuration
app.otp.expiry-minutes=10
